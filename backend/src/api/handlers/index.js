/**
 * Handlers barrel export
 */

const ValidationHandler = require('./ValidationHandler')
const AuthHandler = require('./AuthHandler')
const GestureHandler = require('./GestureHandler')
const DrawingHandler = require('./DrawingHandler')
const CursorHandler = require('./CursorHandler')
const MusicalHandler = require('./MusicalHandler')

module.exports = {
  ValidationHandler,
  AuthHandler,
  GestureHandler,
  DrawingHandler,
  CursorHandler,
  MusicalHandler
}
